apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: cilupbah-redis-data
  namespace: cilupbah
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cilupbah-redis
  namespace: cilupbah
spec:
  replicas: 1
  strategy:
    type: Recreate # penting: PVC-nya ReadWriteOnce, jangan rolling update
  selector:
    matchLabels:
      app: cilupbah-redis
  template:
    metadata:
      labels:
        app: cilupbah-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          # Redis ini menyimpan queue dan metadata Horizon. Queue tidak boleh dievict.
          # Headroom antara maxmemory dan cgroup limit diperlukan untuk allocator/AOF.
          command:
            - redis-server
            - --maxmemory
            - "1280mb"
            - --maxmemory-policy
            - noeviction
            - --appendonly
            - "yes"
            - --appendfsync
            - everysec
            - --auto-aof-rewrite-percentage
            - "100"
            - --auto-aof-rewrite-min-size
            - 64mb
            - --dir
            - /data
          ports:
            - containerPort: 6379
          volumeMounts:
            - name: data
              mountPath: /data
          resources:
            requests:
              memory: "1Gi"
              cpu: "100m"
            limits:
              memory: "2Gi"
              cpu: "500m"
          livenessProbe:
            exec:
              command: ["redis-cli", "ping"]
            initialDelaySeconds: 15
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            exec:
              command: ["redis-cli", "ping"]
            initialDelaySeconds: 5
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: cilupbah-redis-data
---
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: cilupbah
spec:
  selector:
    app: cilupbah-redis
  ports:
    - port: 6379
      targetPort: 6379
